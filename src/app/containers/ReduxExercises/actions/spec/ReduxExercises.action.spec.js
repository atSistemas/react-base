import { expect } from 'chai';
import * as Actions from '../';
import { generateFetchTypes } from 'base/shared/TypeHelper';

import api from '../../api';

describe('Actions', () => {
  describe('Actions ReduxExercises', () => {
    it('Should create action getReduxExercises', () => {

   /*   const params = {};
      const expectedAction = {
        types: generateFetchTypes(Types.REDUXEXERCISES_REQUEST),
        request: api.fetchReduxExercises(params)
      };

      expect(Actions.getReduxExercises(params)).toEqual(expectedAction);
*/
    });

  });
});
