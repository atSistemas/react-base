import { expect } from 'chai';
import * as Actions from '../';
import { generateFetchTypes } from 'base/shared/TypeHelper';

import api from '../../api';

describe('Actions', () => {
  describe('Actions ImmutableCourse', () => {
    it('Should create action getImmutableCourse', () => {

   /*   const params = {};
      const expectedAction = {
        types: generateFetchTypes(Types.IMMUTABLECOURSE_REQUEST),
        request: api.fetchImmutableCourse(params)
      };

      expect(Actions.getImmutableCourse(params)).toEqual(expectedAction);
*/
    });

  });
});
