import { expect } from 'chai';
import reducer from '..';

describe('Reducers', () => {
  describe('ImmutableCourse reducer', () => {

   /* it('Should return the initial state', () => {

      const action = {
        type: types.IMMUTABLECOURSE_REQUEST
      };

      expect(reducer([], action)).toEqual([]);
    });*/

  /*  it('should return the state of ImmutableCourse request fail', () => {

      const action = {
        type: types.IMMUTABLECOURSE_ERROR
      };

      expect(reducer([], action)).toEqual([]);
    });*/
  });
});
