import { expect } from 'chai';
import reducer from '..';

describe('Reducers', () => {
  describe('ImmutableExercises reducer', () => {

   /* it('Should return the initial state', () => {

      const action = {
        type: types.IMMUTABLEEXERCISES_REQUEST
      };

      expect(reducer([], action)).toEqual([]);
    });*/

  /*  it('should return the state of ImmutableExercises request fail', () => {

      const action = {
        type: types.IMMUTABLEEXERCISES_ERROR
      };

      expect(reducer([], action)).toEqual([]);
    });*/
  });
});
