import { expect } from 'chai';
import * as Actions from '../';
import { generateFetchTypes } from 'base/shared/TypeHelper';

import api from '../../api';

describe('Actions', () => {
  describe('Actions ImmutableExercises', () => {
    it('Should create action getImmutableExercises', () => {

   /*   const params = {};
      const expectedAction = {
        types: generateFetchTypes(Types.IMMUTABLEEXERCISES_REQUEST),
        request: api.fetchImmutableExercises(params)
      };

      expect(Actions.getImmutableExercises(params)).toEqual(expectedAction);
*/
    });

  });
});
