import { expect } from 'chai';
import * as Actions from '../';
import { generateFetchTypes } from 'base/shared/TypeHelper';

import api from '../../api';

describe('Actions', () => {
  describe('Actions Ecma6', () => {
    it('Should create action getEcma6', () => {

   /*   const params = {};
      const expectedAction = {
        types: generateFetchTypes(Types.ECMA6_REQUEST),
        request: api.fetchEcma6(params)
      };

      expect(Actions.getEcma6(params)).toEqual(expectedAction);
*/
    });

  });
});
