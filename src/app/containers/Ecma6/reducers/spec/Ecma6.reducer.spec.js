import { expect } from 'chai';
import reducer from '..';

describe('Reducers', () => {
  describe('Ecma6 reducer', () => {

   /* it('Should return the initial state', () => {

      const action = {
        type: types.ECMA6_REQUEST
      };

      expect(reducer([], action)).toEqual([]);
    });*/

  /*  it('should return the state of Ecma6 request fail', () => {

      const action = {
        type: types.ECMA6_ERROR
      };

      expect(reducer([], action)).toEqual([]);
    });*/
  });
});
