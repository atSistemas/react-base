import { expect } from 'chai';
import reducer from '..';

describe('Reducers', () => {
  describe('ReduxExercises reducer', () => {

   /* it('Should return the initial state', () => {

      const action = {
        type: types.REDUXEXERCISES_REQUEST
      };

      expect(reducer([], action)).toEqual([]);
    });*/

  /*  it('should return the state of ReduxExercises request fail', () => {

      const action = {
        type: types.REDUXEXERCISES_ERROR
      };

      expect(reducer([], action)).toEqual([]);
    });*/
  });
});
