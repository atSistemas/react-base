import { expect } from 'chai';
import * as Actions from '../';
import { generateFetchTypes } from 'base/shared/TypeHelper';

import api from '../../api';

describe('Actions', () => {
  describe('Actions TestingExercises', () => {
    it('Should create action getTestingExercises', () => {

   /*   const params = {};
      const expectedAction = {
        types: generateFetchTypes(Types.TESTINGEXERCISES_REQUEST),
        request: api.fetchTestingExercises(params)
      };

      expect(Actions.getTestingExercises(params)).toEqual(expectedAction);
*/
    });

  });
});
