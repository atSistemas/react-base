import { expect } from 'chai';
import reducer from '..';

describe('Reducers', () => {
  describe('TestingExercises reducer', () => {

   /* it('Should return the initial state', () => {

      const action = {
        type: types.TESTINGEXERCISES_REQUEST
      };

      expect(reducer([], action)).toEqual([]);
    });*/

  /*  it('should return the state of TestingExercises request fail', () => {

      const action = {
        type: types.TESTINGEXERCISES_ERROR
      };

      expect(reducer([], action)).toEqual([]);
    });*/
  });
});
