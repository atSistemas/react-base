import { generateMap } from 'base';
import fetch from 'isomorphic-fetch';
import { ReactCourseModel } from '../models';
/*
const url = 'http://localhost:8000/mocks/logo.json';

export default {

  fetchReactCourse() {
    return fetch(url)
    .then(req => req.json())
    .then(data => generateMap(data, ReactCourseModel));

          .then(req => req.json())
      .then(data => this.setState({stations: data}))
      .catch(err => console.log(err));
  }

};*/
