import { expect } from 'chai';
import reducer from '..';

describe('Reducers', () => {
  describe('ReactCourse reducer', () => {

   /* it('Should return the initial state', () => {

      const action = {
        type: types.REACTCOURSE_REQUEST
      };

      expect(reducer([], action)).toEqual([]);
    });*/

  /*  it('should return the state of ReactCourse request fail', () => {

      const action = {
        type: types.REACTCOURSE_ERROR
      };

      expect(reducer([], action)).toEqual([]);
    });*/
  });
});
