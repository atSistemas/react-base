import { expect } from 'chai';
import * as Actions from '../';
import { generateFetchTypes } from 'base/shared/TypeHelper';

import api from '../../api';

describe('Actions', () => {
  describe('Actions ReactCourse', () => {
    it('Should create action getReactCourse', () => {

   /*   const params = {};
      const expectedAction = {
        types: generateFetchTypes(Types.REACTCOURSE_REQUEST),
        request: api.fetchReactCourse(params)
      };

      expect(Actions.getReactCourse(params)).toEqual(expectedAction);
*/
    });

  });
});
