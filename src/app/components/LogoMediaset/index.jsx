import React  from 'react';
import { PropTypes } from 'prop-types';
import { Link } from 'react-router';

const MediasetLogo = ( ) => (
   
   <img src="assets/images/logo-mediaset.png" />

);

export default MediasetLogo;