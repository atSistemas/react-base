import React, { Component } from 'react'
import Actions from '../actions/actions'

const ReactBaseLogo = ({ onClick }) => (

  <img onClick="{ onClick }" src="./react-base-logo.png" alt="ReactBase" width="500" />

)

export default ReactBaseLogo
